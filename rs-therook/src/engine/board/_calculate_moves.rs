use super::*;

impl Board {
    pub fn calculate_moves(&self) -> Vec<Move> {
        timed!("calculated board", {
            let moves: Vec<Move> = vec![];

            // Calculate

            moves
        })
    }
}
