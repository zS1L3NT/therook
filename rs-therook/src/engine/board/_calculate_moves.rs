use therook::timed;

use super::*;

impl Board {
    #[timed(Board)]
    pub fn calculate_moves(&self) -> Vec<Move> {
        let moves: Vec<Move> = vec![];

        // Calculate

        moves
    }
}
